#+TODO: TODO IN_PROGRESS REVIEW | DONE

* IN_PROGRESS Write public API documentation using odoc with examples
:PROPERTIES:
:ID: ecs-001
:END:

** Tasks
- [X] Document eon_ecs.mli and give meaningfull usage examples in the documentation
- [X] Document all other public facing modules that are exported through eon_ecs.mli
- [X] Write a tutorial style documentation in docs using markdown format about eon_ecs usage
- [ ] Add CI job `docs` using odoc
- [ ] Publish the `_build/default/_doc/_html` directory using `peaceiris/actions-gh-pages`
- [ ] Add a badge/link to README

** Notes
- Expanded public API docs and usage examples in `eon_ecs/eon_ecs.mli` for all exported modules (`Entity_id`, `Component`, `World`, `Query`, `Clock`, `Bus`, `Signals`, `Events`, `Commands`, `System`, `Pipeline`, `Progress`, `Loop`).
- Improved module-level/public docs in:
  - `eon_ecs/world.mli`
  - `eon_ecs/query.mli`
  - `eon_ecs/component.mli`
  - `eon_ecs/entity_id.mli`
  - `eon_ecs/clock.mli`
  - `eon_ecs/bus.mli`
  - `eon_ecs/single_bus.mli`
  - `eon_ecs/double_bus.mli`
  - `eon_ecs/progress.mli`
  - `eon_ecs/loop.mli`
- Added documentation to public signatures exported from `.ml` modules:
  - `eon_ecs/system.ml`
  - `eon_ecs/pipeline.ml`
- Added tutorial-style documentation file: `docs/eon_ecs_usage_tutorial.md`.
- Validation performed:
  - `just build` (pass)
  - `opam exec -- dune build @doc` (pass; existing odoc warning about hidden fields in `Component.component` include expansion)
  - `just run-tests` (pass; 69 tests).

* DONE eon_engine components
:PROPERTIES:
:ID: ecs-009
:LAST_UPDATED: 2026-03-01 12:36
:END:

** Tasks
- [X] finalize v1 component policy (no built-in physics, no hot-loading, fast full-reload)
- [X] split component planning into focused TODOs by group
- [X] keep registration API tracking in dedicated task (`ecs-010`)

** Notes
- Expanded `ecs-009` into a discussion-first checklist before writing code.
- Added candidate component set and categorization goals for a 2D game ECS design pass.
- Added dedicated `Camera` component candidate, keeping `CameraTarget` as a separate follow-target concern.
- Added `Mana` as the default secondary resource candidate alongside `Health`, keeping it minimal (`current`, `max`) without baked-in regen.
- Kept collision defaults engine-agnostic by expanding `Collider` filtering/static fields and removing `RigidBody` from required defaults.
- Confirmed physics scope: eon_engine will not ship built-in physics; users can integrate their own physics engine.
- Added `Owner` as a default candidate for summon/projectile attribution and allegiance linkage.
- Moved detailed component registration API requirements to `ecs-010` to keep this task focused on component definitions.
- Confirmed runtime policy: no hot-loading in v1; rely on fast full-reload and boot-time-only component registration.
- Split execution scope into grouped tasks: `ecs-011` (core), `ecs-012` (rendering), `ecs-013` (collision), `ecs-014` (input), `ecs-015` (lifecycle/metadata).
- No implementation files changed; planning scope remains under `eon_engine/components/`.

* TODO eon_engine component registration API
:PROPERTIES:
:ID: ecs-010
:LAST_UPDATED: 2026-03-01 12:12
:END:

** Tasks
- [ ] define component registration API
  - [ ] define typed key model as open variant under `Eon_engine.Components` (`type _ component_key = ..`)
  - [ ] each built-in component has its own key constructor
  - [ ] `Eon_engine.Components.register_one world key`
  - [ ] `Eon_engine.Components.register world keys`
  - [ ] `Eon_engine.Components.register_all world`
  - [ ] registration is world-scoped (no hidden global registry)
  - [ ] registration operations are idempotent
  - [ ] duplicate key with different payload type returns explicit error
  - [ ] `register_all` includes only default components (no game-specific extras)

** Notes
- Split out from `ecs-009` so registration API design can be tracked independently.
- Registration should use typed component keys from an open variant (`Eon_engine.Components.component_key`) so built-ins are strongly typed and users can extend with custom keys.

* TODO eon_engine core gameplay components
:PROPERTIES:
:ID: ecs-011
:LAST_UPDATED: 2026-03-01 12:36
:END:

** Tasks
- [ ] define final core component set for v1
- [ ] define minimal field schema and defaults
- [ ] map each component to files under `eon_engine/components/`
- [ ] add one typed key per core component under `Eon_engine.Components.component_key`
- [ ] draft components:
  - [ ] Position `{ x, y }`
  - [ ] Velocity `{ dx, dy }`
  - [ ] Acceleration `{ ax, ay }`
  - [ ] Rotation `{ angle }`
  - [ ] Scale `{ x, y }`
  - [ ] Health `{ current, max }`
  - [ ] Mana `{ current, max }`
  - [ ] Team `{ id }`
  - [ ] Owner `{ entity_id }`

** Notes
- Split from `ecs-009` to isolate core gameplay component design.

* TODO eon_engine rendering components
:PROPERTIES:
:ID: ecs-012
:LAST_UPDATED: 2026-03-01 12:36
:END:

** Tasks
- [ ] define final rendering component set for v1
- [ ] define minimal field schema and defaults
- [ ] map each component to files under `eon_engine/components/`
- [ ] add one typed key per rendering component under `Eon_engine.Components.component_key`
- [ ] draft components:
  - [ ] Sprite `{ texture_id, layer, flip_x, flip_y }`
  - [ ] Animation `{ clip, frame, speed, playing }`
  - [ ] Camera `{ zoom, viewport_w, viewport_h, near, far }`
  - [ ] CameraTarget `{ priority, offset_x, offset_y }`

** Notes
- Split from `ecs-009` to isolate rendering component design.

* TODO eon_engine collision components
:PROPERTIES:
:ID: ecs-013
:LAST_UPDATED: 2026-03-01 12:36
:END:

** Tasks
- [ ] define collision component set for v1 (engine-agnostic)
- [ ] define minimal field schema and defaults
- [ ] map each component to files under `eon_engine/components/`
- [ ] add one typed key per collision component under `Eon_engine.Components.component_key`
- [ ] draft components:
  - [ ] Collider `{ shape, layer, mask, is_trigger, is_static }`
- [ ] document collision policy:
  - [ ] no built-in physics in eon_engine
  - [ ] users can integrate external physics engines (e.g. Box2D)

** Notes
- Split from `ecs-009` to isolate collision design and policy.

* TODO eon_engine input and interaction components
:PROPERTIES:
:ID: ecs-014
:LAST_UPDATED: 2026-03-01 12:36
:END:

** Tasks
- [ ] define input/interaction component set for v1
- [ ] define minimal field schema and defaults
- [ ] map each component to files under `eon_engine/components/`
- [ ] add one typed key per input/interaction component under `Eon_engine.Components.component_key`
- [ ] draft components:
  - [ ] Input `{ move_x, move_y, actions }`
- [ ] document pipeline usage:
  - [ ] input phase runs before gameplay phase
  - [ ] gameplay systems read input intent and apply behavior

** Notes
- Split from `ecs-009` to isolate input/interaction design.

* TODO eon_engine lifecycle and metadata components
:PROPERTIES:
:ID: ecs-015
:LAST_UPDATED: 2026-03-01 12:36
:END:

** Tasks
- [ ] define lifecycle/metadata component set for v1
- [ ] define minimal field schema and defaults
- [ ] map each component to files under `eon_engine/components/`
- [ ] add one typed key per lifecycle/metadata component under `Eon_engine.Components.component_key`
- [ ] draft components:
  - [ ] Lifetime `{ ttl }`
  - [ ] Tag `{ value }`
- [ ] confirm runtime policy:
  - [ ] component type registration is boot-time only
  - [ ] component registry is immutable during runtime
  - [ ] v1 does not support hot-loading; use fast full-reload workflow

** Notes
- Split from `ecs-009` to isolate lifecycle/metadata design and runtime constraints.
