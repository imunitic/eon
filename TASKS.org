#+TODO: TODO IN_PROGRESS REVIEW | DONE

* IN_PROGRESS Write public API documentation using odoc with examples
:PROPERTIES:
:ID: ecs-001
:END:

** Tasks
- [X] Document eon_ecs.mli and give meaningfull usage examples in the documentation
- [X] Document all other public facing modules that are exported through eon_ecs.mli
- [X] Write a tutorial style documentation in docs using markdown format about eon_ecs usage
- [ ] Add CI job `docs` using odoc
- [ ] Publish the `_build/default/_doc/_html` directory using `peaceiris/actions-gh-pages`
- [ ] Add a badge/link to README

** Notes
- Expanded public API docs and usage examples in `eon_ecs/eon_ecs.mli` for all exported modules (`Entity_id`, `Component`, `World`, `Query`, `Clock`, `Bus`, `Signals`, `Events`, `Commands`, `System`, `Pipeline`, `Progress`, `Loop`).
- Improved module-level/public docs in:
  - `eon_ecs/world.mli`
  - `eon_ecs/query.mli`
  - `eon_ecs/component.mli`
  - `eon_ecs/entity_id.mli`
  - `eon_ecs/clock.mli`
  - `eon_ecs/bus.mli`
  - `eon_ecs/single_bus.mli`
  - `eon_ecs/double_bus.mli`
  - `eon_ecs/progress.mli`
  - `eon_ecs/loop.mli`
- Added documentation to public signatures exported from `.ml` modules:
  - `eon_ecs/system.ml`
  - `eon_ecs/pipeline.ml`
- Added tutorial-style documentation file: `docs/eon_ecs_usage_tutorial.md`.
- Validation performed:
  - `just build` (pass)
  - `opam exec -- dune build @doc` (pass; existing odoc warning about hidden fields in `Component.component` include expansion)
  - `just run-tests` (pass; 69 tests).

* DONE Add a generate-docs task to just
:PROPERTIES:
:ID: ecs-004
:END:

** Tasks

- [X] Add a generate-docs task to just that will use odoc and generate documentation

** Notes
- Added `generate-docs` recipe in `Justfile`:
  - `opam exec -- dune build @doc`
- Validation:
  - `just generate-docs` (pass)

* DONE Write a snake game example using non-reactive style (direct updates)
:PROPERTIES:
:ID: ecs-002
:LAST_UPDATED: 2026-02-21 20:10
:END:

** Goal
- [X] Implement a classic Snake game using notty as the rendering backend
- [X] Follow a non-reactive ECS style
- [X] Place the example in: eon_ecs/examples/snake_nonreactive
- [X] add a just task called snake_nonreactive to run the game
- [X] When the game ends it restarts automatically, and pressing `q` exits the game
- [X] Pause feature: pressing `p` pauses/unpauses gameplay without input/timing issues
- [X] Draw walls as gray squares around the game field

** Grid
- [X] Use a fixed grid size of 40x20 cells
- [X] Each cell represents one logical game unit

** Snake Model
- [X] Represent the snake as an ordered list of grid positions
- [X] The first element in the list is the head
- [X] Initial snake length is 3 segments
- [X] The snake starts centered on the grid moving right
- [X] Represent direction as a 2D unit vector:
      - Up    = (0, -1)
      - Down  = (0,  1)
      - Left  = (-1, 0)
      - Right = (1,  0)
- [X] On each update tick:
      - Compute new_head = current_head + direction
      - Insert new_head at the front of the list
      - Remove the last segment unless food was eaten

** Movement Rules
- [X] Velocity is constant and equal to 1 cell per tick
- [X] Use a fixed timestep of 100ms per update
- [X] Input must be handled non-blocking
- [X] Direction is controlled by arrow keys or W, A, S, D
- [X] 180-degree turns are not allowed

** Food Rules
- [X] Only one food exists at a time
- [X] Food spawns at a random grid position not occupied by the snake
- [X] When food is eaten:
      - Do not remove the tail on that tick (snake grows by 1)
      - Spawn new food at a new valid random position

** Collision Rules
- [X] Hitting a wall ends the game
- [X] Hitting the snake’s own body ends the game
- [X] No wall wrapping is allowed

** Rendering
- [X] Render snake segments as white squares
- [X] Render food as a distinct colored square
- [X] Clear and redraw the grid each frame

** Architecture Constraints
- [X] Non-reactive means:
      - No signals
      - No event buses
      - No command queues
      - No reactive streams
- [X] Systems directly mutate components inside the main update loop
- [X] The game loop explicitly calls update systems and render systems

** Notes
- Added non-reactive Snake example in `eon_ecs/examples/snake_nonreactive.ml` with `notty` rendering, run target `just snake_nonreactive`, and fixed-step gameplay (`0.125s`).
- Refactored component model to clearer ECS names: `Trail`, `Direction`, `Alive`, and generic `Position` for food.
- Kept game state in world data/resources (`paused`, `quit`, `score`, RNG, `any_alive`) and removed unnecessary tag/id bookkeeping.
- Introduced dedicated `input_system` (variable tick) that drains non-blocking terminal input and writes world state (`Direction`, pause/quit flags); renderer is now render-only.
- Pipeline now uses explicit phase ordering with dependencies: `Input -> Gameplay` via `Pipeline.before`, with gameplay systems handling movement and alive aggregation.
- Movement system enforces authoritative anti-180 behavior in simulation and applies collision/growth/food-spawn updates directly on queried entities.
- Restart behavior now rebuilds a fresh world/session when snake dies (app-level orchestration), while `q`/`Esc` exits immediately.
- Added pause toggle (`p`) without accumulated tick jump on resume and wall rendering as gray border tiles.
- Improved example readability with architecture comments, non-trivial logic notes, and grouped sections in the file.
- Validation: `just build` (pass).
- Validation: `just run-tests` (pass).

* DONE Add optional initial render to Loop.run
:PROPERTIES:
:ID: ecs-008
:LAST_UPDATED: 2026-02-21 19:05
:END:

** Tasks
- [X] Add optional `~render_initial:bool` to `Loop.run` in `eon_ecs/loop.mli` and `eon_ecs/loop.ml` (default `false`).
- [X] When enabled, render one frame with `dt=0.0` before the first simulation step.
- [X] Update `eon_ecs/examples/snake_nonreactive.ml` to use `~render_initial:true` and remove manual pre-loop render call.
- [X] Document rationale: this removes per-game ad-hoc first-frame render calls and keeps initial-frame behavior in the loop abstraction.

** Notes
- Added `render_initial` as an optional `Loop.run` argument to support first-frame rendering through loop API instead of game-specific bootstrap code.
- Updated snake non-reactive example to rely on loop-managed initial render, removing direct `Snake_renderer.render_world` call before `Snake_loop.run`.
- Validation: `just build` (pass).
- Validation: `just run-tests` (pass; 70 tests).

* DONE Enable Heterogeneous Query Payloads
:PROPERTIES:
:ID: ecs-007
:LAST_UPDATED: 2026-02-21 18:43
:END:

** Tasks
- [X] Update `Query.iter2`, `Query.iter3`, and `Query.iter4` signatures to support mixed payload types (`'a`, `'b`, `'c`, `'d`).
- [X] Update query implementation to preserve smallest-base iteration while supporting heterogeneous storages.
- [X] Verify ECS tests and snake example compile/run with heterogeneous query callbacks.

** Notes
- Changed `eon_ecs/query.mli` callback signatures for `iter2/iter3/iter4` to heterogeneous type parameters.
- Updated `eon_ecs/query.ml` with explicit local abstract types and size-based base-set selection logic that works across mixed payload types.
- Refactored `eon_ecs/examples/snake_nonreactive.ml` to use mixed-type `Query.iter2/iter3` in systems and renderer, aligning with shape-based ECS usage.
- Validation: `just build` (pass).
- Validation: `just run-tests` (pass; 70 tests).
  
* DONE Write a snake game example using reactive style (using buses)
:PROPERTIES:
:ID: ecs-003
:LAST_UPDATED: 2026-02-22 00:45
:END:

** Tasks
- [X] do the same as in ecs-002 but using a reactive style
- [X] use the eon_ecs/examples/snake_nonreactive.ml as a blueprint and guide to implement this
- [X] reactive means: mutation happens only in command handlers, in systems only signals and events are published
- [X] add this example in eon_ecs/examples/snake_reactive
- [X] add a just task called snake_reactive to run the game

** Notes
- Updated task scope for the reactive Snake example to explicitly use `eon_ecs/examples/snake_nonreactive.ml` as the blueprint.
- Implemented `eon_ecs/examples/snake_reactive.ml` with bus-driven flow: systems emit signals, signal handlers enqueue commands, and command handlers apply world mutations.
- Wired `Signals`, `Events`, and `Commands` as world services and added loop collect/drain orchestration in the example loop wiring.
- Added executable entry in `eon_ecs/examples/dune` and `just snake_reactive` task in `Justfile`.
- Exposed `Eon_ecs.Loop.Default_buses` in the public API and updated `snake_reactive` loop wiring to use `Eon_ecs.Loop.Progress_adapter` + `Eon_ecs.Loop.Default_buses`.
- Kept `Apply_step` as an entity-targeted command handler using direct `World.get_component` reads, and rewrote the handler in a staged `let ... in` style for readability.
- Validation: `just build` (pass).
- Validation: `just run-tests` (pass).

* DONE Update AGENTS.md and README.md to reflect the latest changes
:PROPERTIES:
:ID: ecs-006
:LAST_UPDATED: 2026-02-21 16:37
:END:

** Tasks

- [X] Update AGENTS.md, trim the content and move the example to README.md. Keep AGENTS.md lean and relevant for AI agents to understand the codebase and rules
- [X] Update README.md to be up to date with the current changes

** Notes
- Trimmed `AGENTS.md` from a long narrative/example-heavy document to a focused agent playbook (public API boundary, invariants, default stack, build/test/bench, contribution/release checklist, commit convention).
- Moved and expanded usage examples in `README.md` (canonical default-stack flow and custom loop renderer wiring) and updated behavior notes to current semantics (bus order, unregistered component raises, pipeline topo cache, resource key identity).
- Validation: `just build` (pass; existing warning 33 in benchmark files).
- Validation: `just run-tests` (pass).

* DONE Check changes that claude code did
:PROPERTIES:
:ID: ecs-005
:LAST_UPDATED: 2026-02-21 16:32
:END:

** Tasks

- [X] Check the last 5 commits and do a review of them. They were done by comrade claude-code.

** Notes
- Reviewed commits: `c3e5c05`, `0a017ac`, `8099467`, `547240d`, `f8263aa` (all co-authored by Claude Sonnet 4.6).
- Focused review on behavior/API risk areas in `eon_ecs/query.ml`, `eon_ecs/resource_store.ml`, `eon_ecs/pipeline.ml`, `eon_ecs/loop.mli`, and `Justfile`.
- Validation performed: `just run-tests` (pass).
